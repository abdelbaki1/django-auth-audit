============
Installation
============

At the command line::

    $ easy_install dj-auth-audit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-auth-audit
    $ pip install dj-auth-audit
